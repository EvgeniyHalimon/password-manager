import { styled, Dialog } from '@mui/material';

export const StyledDialodAddForm = styled(Dialog)(({ theme }) => ({
  '& .MuiFormControl-root': { width: '93%' },
  '& .MuiButtonBase-root': { width: '93%' },
}));

